# 2024 历史房源远端发布报告

## 目标

- Supabase 项目：`zaneyang1`
- Project ref：`paetpneqyherfwutisdj`
- Region：`ap-northeast-1`
- 发布批次：`5b640272-0dec-5dd8-b12a-45786ef1a60e`
- 数据周期：`2024-11`

## 迁移

远端原有迁移为 `202608050001` 至 `202608120012`。先执行 dry-run，只发现以下两个待应用迁移：

- `202608120013_historical_housing.sql`
- `202608120014_housing_import_observability.sql`

应用后再次读取远端 migration list，本地与远端 14 个迁移全部一致。

## 导入与校验

- 源记录：60,202
- 接受：60,202
- 拒绝：0
- 导入：60,202
- 远端实际：60,202
- 源文件前后 SHA-256 一致：是
- 本地与远端内容校验和一致：是
- 内容校验和：`3e3173b992adc372ddb262dd4b9ecd6b3444393c88cb281c32b071500cf672ae`
- 激活后数据库大小：70,216,851 bytes
- 激活保护线：400 MB
- 最终状态：`active`

再次执行相同导入返回 `resumed: true` 和 `already_active: true`，证明发布操作可安全重跑且不会重复生成房源。

## 查询与权限验证

使用服务端 secret 调用 `search_historical_houses`：

- HTTP 200
- 武林广场 5 公里、月租不超过 3500 元、一居室条件返回 3 条样例
- 条件总匹配数 1,497
- 最近样例约 318 米
- 数据周期为 `2024-11`

使用浏览器 publishable key 调用相同 RPC：

- HTTP 401
- 公开调用被阻止

因此历史房源只能经过服务端工具调用，不会把整库查询权限或服务端 secret 下发给浏览器。

## 真实性边界

这批数据是经授权使用的过期历史快照，只能说明 2024 年 11 月的记录。产品必须持续展示历史来源和“不代表当前仍可出租或当前价格”的提示。
